@model DuLich.Models.TourDuLich

@{
    ViewBag.Title = "Cập Nhật Ảnh Tour Du lịch";
    Layout = "~/Areas/Admin/Views/Shared/layoutadmin.cshtml";
}

<div class="row" style="margin-top:50px">
    <div class="col-md-12">
        <h4>
            Cập Nhật Hình Ảnh: @Model.ID
        </h4>
        @*<form action="/Admin/TaiKhoan/CapNhatAnh" method="post" enctype="multipart/form-data"></form>*@
        @using (Html.BeginForm("CapNhatAnh", "Tour", FormMethod.Post, new { enctype = "multipart/form-data" }))
        {
            @Html.AntiForgeryToken()
            <input type="hidden" name="idTour" value="@Model.ID" />
            <div class="form-horizontal">
                <hr />
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                <div class="form-group">
                    <label class="control-label col-md-2">Chọn file ảnh</label>
                    <div class="col-md-10">
                        <input type="file" name="imgTour" />
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-md-offset-2 col-md-10">
                        <input type="submit" value="Save" class="btn btn-default" />
                    </div>
                </div>
                <p style="color:red">@ViewBag.error</p>
            </div>
        }
    </div>
</div>

